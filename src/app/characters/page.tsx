import { CharactersView } from "@/components/studio/characters-view";

export const metadata = { title: "Lillyput — Characters" };

export default function CharactersPage() {
  return <CharactersView />;
}
