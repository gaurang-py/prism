import type { ReactNode } from "react";

export const metadata = { title: "Reset password — Prism" };

export default function ResetLayout({ children }: { children: ReactNode }) {
  return children;
}
