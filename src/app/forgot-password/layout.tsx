import type { ReactNode } from "react";

export const metadata = { title: "Forgot password — Lillyput" };

export default function ForgotLayout({ children }: { children: ReactNode }) {
  return children;
}
